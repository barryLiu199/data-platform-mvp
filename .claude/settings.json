{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=\"$TOOL_INPUT\"; if echo \"$CMD\" | grep -qE \"git commit\"; then echo \"[IMPORTANT] 检测到 git commit，请先对当前暂存的改动执行架构评审（参考 .claude/commands/pre-commit-review.md 的检查清单），输出评审报告后再决定是否继续提交。如果评审发现【必须修改】的问题，请告知用户并建议先修复再提交。\"; fi'"
          }
        ]
      }
    ]
  }
}
